export enum ROUTES {
  HOME = '',
  LOGIN = 'login',
  PANEL = 'panel',
  SUBMIT_FORM = 'submit-form',
  FIND_LOST_PET = 'find-lost-pet',
}

export enum SUB_ROUTES {
  PRESENT = 'present',
  HISTORY = 'history',
}
